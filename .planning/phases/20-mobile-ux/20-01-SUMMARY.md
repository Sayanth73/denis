---
phase: 20-mobile-ux
plan: 01
subsystem: ui
tags: [mobile, responsive, tailwind, shadcn, sheet, hamburger, nav]

# Dependency graph
requires:
  - phase: layout-shell
    provides: sidebar, header, nav-item, nav.ts, nav-icons.tsx
provides:
  - Sheet-based mobile navigation drawer (hamburger + all 9 nav items)
  - Responsive sidebar (hidden on mobile via hidden md:flex)
  - Responsive layout offset (md:pl-60 — zero on mobile, 240px on desktop)
  - Horizontal scroll on all 5 data tables (overflow-x-auto)
  - Mobile-safe dialog widths (mx-4 w-[calc(100%-2rem)]) on all 5 dialogs
affects: [all pages — layout, navigation, tables, dialogs]

# Tech tracking
tech-stack:
  added: [shadcn Sheet component]
  patterns: [md: breakpoint-only responsive layer, event delegation for Sheet close, overflow-x-auto on table wrappers, mx-4 w-[calc(100%-2rem)] on dialogs]

key-files:
  created:
    - components/ui/sheet.tsx
    - components/layout/mobile-nav.tsx
  modified:
    - components/layout/sidebar.tsx
    - components/layout/header.tsx
    - app/layout.tsx
    - components/clients/clients-table.tsx
    - components/production/ordre-fabrication-table.tsx
    - components/livraisons/deliveries-table.tsx
    - components/matieres/raw-materials-table.tsx
    - app/factures/page.tsx
    - components/clients/client-dialog.tsx
    - components/production/recette-dialog.tsx
    - components/production/production-wizard.tsx
    - components/livraisons/new-delivery-dialog.tsx
    - components/matieres/reception-dialog.tsx

key-decisions:
  - "MobileNav uses Sheet side=left with w-60 p-0 to mirror desktop sidebar position and width"
  - "Sheet close uses nav onClick event delegation — avoids adding onClick to NavItemProps"
  - "Hamburger wrapped in md:hidden div in header — invisible on desktop, no layout impact"
  - "overflow-x-auto + overflow-hidden coexist on table wrapper: horizontal scroll, vertical clip preserved for rounded-md"
  - "mx-4 w-[calc(100%-2rem)] as first classes before sm:max-w-* — applies on mobile, overridden by sm: on wider viewports"
  - "production-wizard step 3 outer div gets min-w-0 to prevent flex overflow on narrow screens"

patterns-established:
  - "Mobile-only UI: wrap in md:hidden; desktop-only UI: use hidden md:flex"
  - "Sheet drawer from shadcn for full-height side panels on mobile"
  - "Table mobile scroll: overflow-x-auto overflow-hidden on wrapper div"
  - "Dialog mobile width: mx-4 w-[calc(100%-2rem)] prepended to DialogContent className"

requirements-completed:
  - REQ-v5-mobile-nav
  - REQ-v5-mobile-tables
  - REQ-v5-mobile-dialogs
  - REQ-v5-mobile-layout

# Metrics
duration: 3min
completed: 2026-05-05
---

# Phase 20 Plan 01: Mobile UX Summary

**shadcn Sheet drawer + Tailwind md: breakpoints deliver full iPhone 390px usability — hamburger nav, scrollable tables, safe-width dialogs — without touching desktop layout**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-05T20:40:07Z
- **Completed:** 2026-05-05T20:43:00Z
- **Tasks:** 3
- **Files modified:** 14

## Accomplishments
- Installed shadcn Sheet and created MobileNav (55 lines) with hamburger trigger and event-delegated Sheet close
- Wired responsive layout: sidebar hidden on mobile (hidden md:flex), header gains hamburger (md:hidden), layout offset is md:pl-60
- Added overflow-x-auto to all 5 data table wrappers for horizontal scroll on 390px
- Added mx-4 w-[calc(100%-2rem)] to all 5 DialogContent elements for viewport-safe dialog widths
- Added min-w-0 to production wizard step 3 to prevent flex child overflow

## Task Commits

Each task was committed atomically:

1. **Task 1: Install Sheet and create MobileNav component** - `6b76df9` (feat)
2. **Task 2: Wire MobileNav into layout — sidebar, header, and layout offset** - `785b2f8` (feat)
3. **Task 3: Fix table overflow and dialog widths** - `a43018b` (feat)

**Plan metadata:** committed with SUMMARY.md (docs)

## Files Created/Modified
- `components/ui/sheet.tsx` - shadcn Sheet component (generated by CLI)
- `components/layout/mobile-nav.tsx` - new MobileNav: hamburger trigger + Sheet drawer with all 9 nav items
- `components/layout/sidebar.tsx` - aside flex -> hidden md:flex (invisible on mobile)
- `components/layout/header.tsx` - added MobileNav import and md:hidden wrapper
- `app/layout.tsx` - pl-60 -> md:pl-60 (zero offset on mobile)
- `components/clients/clients-table.tsx` - overflow-x-auto added to table wrapper
- `components/production/ordre-fabrication-table.tsx` - overflow-x-auto added to table wrapper
- `components/livraisons/deliveries-table.tsx` - overflow-x-auto added to table wrapper
- `components/matieres/raw-materials-table.tsx` - overflow-x-auto added to table wrapper
- `app/factures/page.tsx` - overflow-x-auto added to table wrapper
- `components/clients/client-dialog.tsx` - mx-4 w-[calc(100%-2rem)] added to DialogContent
- `components/production/recette-dialog.tsx` - mx-4 w-[calc(100%-2rem)] added to DialogContent
- `components/production/production-wizard.tsx` - mx-4 w-[calc(100%-2rem)] added to DialogContent; min-w-0 on step 3 div
- `components/livraisons/new-delivery-dialog.tsx` - mx-4 w-[calc(100%-2rem)] added to DialogContent
- `components/matieres/reception-dialog.tsx` - mx-4 w-[calc(100%-2rem)] added to DialogContent

## Decisions Made
- Used event delegation (`<nav onClick={() => setOpen(false)}>`) to close Sheet on nav item click without modifying NavItemProps
- Prepended mx-4 w-[calc(100%-2rem)] before sm:max-w-* so mobile classes apply below 640px breakpoint
- min-w-0 on production wizard step 3 guards flex children from overflowing narrow viewports

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Mobile UX complete: 390px viewport fully usable for all navigation, tables, and dialogs
- Desktop layout identical to pre-phase state (all changes behind md: breakpoint)
- No blockers for subsequent phases

## Self-Check

- [x] components/ui/sheet.tsx exists
- [x] components/layout/mobile-nav.tsx exists (55 lines)
- [x] All 5 table wrappers have overflow-x-auto overflow-hidden
- [x] All 5 dialogs have mx-4 w-[calc(100%-2rem)]
- [x] Sidebar has hidden md:flex
- [x] Header has MobileNav (md:hidden wrapper)
- [x] Layout has md:pl-60
- [x] npx tsc --noEmit exits clean
- [x] No file exceeds 300 lines
- [x] Commits 6b76df9, 785b2f8, a43018b exist

## Self-Check: PASSED

---
*Phase: 20-mobile-ux*
*Completed: 2026-05-05*
