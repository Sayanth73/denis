---
phase: 03-matieres-premieres-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components.json
  - components/ui/table.tsx
  - components/ui/dialog.tsx
  - components/ui/input.tsx
  - components/ui/select.tsx
  - components/ui/label.tsx
  - components/ui/form.tsx
  - components/ui/badge.tsx
  - components/ui/calendar.tsx
  - components/ui/popover.tsx
  - components/ui/command.tsx
  - components/ui/date-picker.tsx
  - components/ui/combobox.tsx
  - components/dlc-badge.tsx
  - components/empty-state.tsx
  - lib/raw-materials.ts
autonomous: true
requirements:
  - REQ-raw-materials-list
  - REQ-raw-material-receive
must_haves:
  truths:
    - "shadcn Table, Dialog, Input, Select, Label, Form, Badge, Calendar, Popover, Command primitives are installed under components/ui/"
    - "react-hook-form, @hookform/resolvers, zod, date-fns, react-day-picker peer-deps are installed in package.json"
    - "<DlcBadge value={iso} /> renders the locked Phase 3 wrapper classes for green/orange/red/grey buckets and formats the date as JJ.MM.AAAA"
    - "<EmptyState /> renders icon + heading + body + optional CTA with the locked dashed-border layout"
    - "<DatePicker /> mounts a shadcn Popover containing a shadcn Calendar with the fr locale and a Button trigger"
    - "<Combobox /> mounts a shadcn Popover containing a shadcn Command list with searchable options and free-text fallback"
    - "lib/raw-materials.ts exports deriveStatut(rm, today), getSupplierOptions(rms), formatDate(iso), TYPE_LABELS, and the StatutValue union"
    - "npx tsc --noEmit exits 0 after these files are added"
  artifacts:
    - path: "components/ui/table.tsx"
      provides: "shadcn Table primitive"
      contains: "Table"
    - path: "components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive"
      contains: "Dialog"
    - path: "components/ui/input.tsx"
      provides: "shadcn Input primitive"
      contains: "Input"
    - path: "components/ui/select.tsx"
      provides: "shadcn Select primitive"
      contains: "Select"
    - path: "components/ui/label.tsx"
      provides: "shadcn Label primitive"
      contains: "Label"
    - path: "components/ui/form.tsx"
      provides: "shadcn Form primitive (react-hook-form wrappers)"
      contains: "FormField"
    - path: "components/ui/badge.tsx"
      provides: "shadcn Badge primitive"
      contains: "Badge"
    - path: "components/ui/calendar.tsx"
      provides: "shadcn Calendar primitive (react-day-picker)"
      contains: "Calendar"
    - path: "components/ui/popover.tsx"
      provides: "shadcn Popover primitive"
      contains: "Popover"
    - path: "components/ui/command.tsx"
      provides: "shadcn Command primitive (cmdk)"
      contains: "Command"
    - path: "components/ui/date-picker.tsx"
      provides: "Reusable DatePicker (Popover + Calendar) with fr locale"
      contains: "DatePicker"
    - path: "components/ui/combobox.tsx"
      provides: "Reusable Combobox (Popover + Command) with free-text fallback"
      contains: "Combobox"
    - path: "components/dlc-badge.tsx"
      provides: "Reusable <DlcBadge value={iso} /> using lib/dlc.ts dlcColor()"
      contains: "DlcBadge"
    - path: "components/empty-state.tsx"
      provides: "Reusable <EmptyState icon heading body cta? /> with dashed border"
      contains: "EmptyState"
    - path: "lib/raw-materials.ts"
      provides: "Pure helpers: deriveStatut, getSupplierOptions, formatDate, TYPE_LABELS"
      contains: "deriveStatut"
  key_links:
    - from: "components/dlc-badge.tsx"
      to: "lib/dlc.ts"
      via: "import { dlcColor } from '@/lib/dlc'"
      pattern: "from \"@/lib/dlc\""
    - from: "components/ui/date-picker.tsx"
      to: "components/ui/calendar.tsx"
      via: "renders <Calendar mode=\"single\" />"
      pattern: "Calendar"
    - from: "components/ui/combobox.tsx"
      to: "components/ui/command.tsx"
      via: "renders <Command><CommandInput><CommandList>"
      pattern: "Command"
    - from: "lib/raw-materials.ts"
      to: "lib/types.ts"
      via: "import type { RawMaterial }"
      pattern: "RawMaterial"
---

<objective>
Install every shadcn primitive Phase 3 needs, add the form/date peer deps, and ship four reusable building blocks (`<DlcBadge>`, `<EmptyState>`, `<DatePicker>`, `<Combobox>`) plus the pure-function helpers `lib/raw-materials.ts`. This plan touches **no route page** — Wave 2 (`03-02-PLAN.md`) consumes everything created here to wire `/matieres-premieres`.

Purpose: Decouple the heavy "install + reusable primitives" chunk from the screen-specific assembly so Wave 2 starts with every dependency in place and can focus on the table + dialog. Splitting this way keeps Wave 1 ≤ 45 % context (no business logic, mostly registry installs and small components) and gives Phases 4-7 the same `DlcBadge`, `EmptyState`, `DatePicker`, and `Combobox` artefacts to inherit (UI-SPEC §Inheritance).

Output: 16 files (10 shadcn primitives, 2 composite UI primitives, 2 reusable presentational components, 1 helper module, 1 amended `package.json`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-matieres-premieres-screen/03-CONTEXT.md
@.planning/phases/03-matieres-premieres-screen/03-UI-SPEC.md
@.planning/phases/01-scaffolding-application-shell/01-UI-SPEC.md
@.planning/phases/02-domain-model-zustand-store-seed-data/02-01-SUMMARY.md

# Existing source the executor will import from
@lib/types.ts
@lib/dlc.ts
@lib/utils.ts
@components/ui/button.tsx

<interfaces>
<!-- Extracted from current codebase. Use these directly — no exploration needed. -->

From lib/types.ts:
```typescript
export type RawMaterial = {
  id: string;
  type: "boeuf" | "agneau" | "poulet" | "epices" | "marinade" | "autre";
  nom: string;
  fournisseur: string;
  numeroLotFournisseur: string;
  quantiteRecue: number;
  quantiteRestante: number;
  dateReception: string; // ISO YYYY-MM-DD
  dlc: string;           // ISO YYYY-MM-DD
  temperatureReception: number;
  certificatSanitaire?: string;
};
```

From lib/dlc.ts:
```typescript
export type DlcColor = "green" | "orange" | "red" | "grey";
export function dlcColor(dlc: string, today: Date): DlcColor;
```

From lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string; // clsx + tailwind-merge
```

From components/ui/button.tsx:
```typescript
export const Button: React.FC<ButtonProps>;
export const buttonVariants: (props: ...) => string;
```

From package.json (already present):
- react ^18, next 14.2.35, tailwindcss ^3.4, lucide-react ^1.14
- @radix-ui/react-alert-dialog, @radix-ui/react-slot, sonner, zustand
- class-variance-authority, clsx, tailwind-merge, tailwindcss-animate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn primitives + form/date peer dependencies</name>
  <files>package.json, package-lock.json, components.json, components/ui/table.tsx, components/ui/dialog.tsx, components/ui/input.tsx, components/ui/select.tsx, components/ui/label.tsx, components/ui/form.tsx, components/ui/badge.tsx, components/ui/calendar.tsx, components/ui/popover.tsx, components/ui/command.tsx</files>
  <action>
Run, in order, from the repository root `/Users/sayanth/Desktop/viande`:

1. `npx shadcn@2.10.0 add table dialog input select label form badge calendar popover command --yes` — same shadcn CLI version Phase 2 used (per 02-01-SUMMARY `npx shadcn@2.10.0 add alert-dialog`). Pin to 2.10.0; do NOT use `@latest`. The `--yes` flag suppresses the overwrite prompts; the existing files (button.tsx, alert-dialog.tsx, sonner.tsx) are NOT in this list so they will not be touched.

2. The shadcn CLI auto-installs Radix peer-deps for the primitives above (`@radix-ui/react-dialog`, `@radix-ui/react-select`, `@radix-ui/react-label`, `@radix-ui/react-popover`, `cmdk`, `react-day-picker`, etc.) into `package.json`. Verify this is the case — if any of those are missing, run `npm install` once afterward.

3. Then install the form + date peer-deps the shadcn `form` and `calendar` blocks expect (these are NOT auto-pulled because they are user-facing libraries, not Radix primitives):

   `npm install react-hook-form @hookform/resolvers zod date-fns`

   `react-day-picker` SHOULD already be present from step 1 (shadcn `calendar` depends on it). If `npm ls react-day-picker` shows it as missing, add it: `npm install react-day-picker`.

4. Do NOT modify any of the generated `components/ui/*.tsx` files. Use them exactly as the shadcn registry produced them. The only file Phase 3 may have to touch in Wave 2 is `lib/utils.ts` — but it is already present from Phase 1 and exports `cn`, so leave it alone.

5. Sanity-check imports compile by running `npx tsc --noEmit`. If a freshly-generated file references a package that did not auto-install (rare — shadcn occasionally leaves out `@radix-ui/react-icons` substitutes), install the missing package and retry.

Why pin shadcn@2.10.0: Phase 2 already shipped one block on this version (`alert-dialog.tsx` is 141 lines and follows the 2.10 layout). Mixing major versions risks generating incompatible primitive layouts. Why not pin react-hook-form / zod / date-fns: these are mature stable libraries; whatever `npm install` resolves on the day of execution is fine — TypeScript will catch breaks immediately via Task 2's `tsc --noEmit`.

Why these 10 shadcn primitives and no others: this is the full set declared in 03-CONTEXT.md §Implementation Decisions and 03-UI-SPEC.md §Components Installed. `tabs` (Phase 4), `textarea` (Phase 5), `checkbox` (Phase 5) are intentionally deferred to their owning phases per Phase 1 inventory.
  </action>
  <verify>
    <automated>cd /Users/sayanth/Desktop/viande &amp;&amp; for f in table dialog input select label form badge calendar popover command; do test -f "components/ui/$f.tsx" || { echo "MISSING components/ui/$f.tsx"; exit 1; }; done &amp;&amp; for pkg in react-hook-form @hookform/resolvers zod date-fns react-day-picker; do node -e "require.resolve('$pkg')" 2>/dev/null || { echo "MISSING dep $pkg"; exit 1; }; done &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>All 10 shadcn primitive files exist under `components/ui/`, the 5 peer-deps resolve via Node, and `npx tsc --noEmit` exits 0. `package.json` includes new deps; `components.json` retains the New York / neutral / CSS-variables config from Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable primitives (DlcBadge, EmptyState, DatePicker, Combobox) + lib/raw-materials.ts helpers</name>
  <files>components/dlc-badge.tsx, components/empty-state.tsx, components/ui/date-picker.tsx, components/ui/combobox.tsx, lib/raw-materials.ts</files>
  <action>
Create five files. All are single-concern, small (≤ ~120 lines each, well under the 300-line cap). Every file uses the existing `cn` from `@/lib/utils` for class composition.

---

**File 1: `components/dlc-badge.tsx`** (per 03-UI-SPEC.md §`<DlcBadge>` — concrete spec). Client component because `dlcColor()` is called at render time against `new Date()`.

```tsx
"use client";

import { cn } from "@/lib/utils";
import { dlcColor, type DlcColor } from "@/lib/dlc";

type DlcBadgeProps = { value: string; className?: string };

const BUCKET_CLASSES: Record<DlcColor, string> = {
  green: "bg-emerald-100 text-emerald-800 border-emerald-200",
  orange: "bg-amber-100 text-amber-800 border-amber-200",
  red: "bg-red-100 text-red-800 border-red-200",
  grey: "bg-zinc-100 text-zinc-600 border-zinc-200",
};

function formatDateJjMmAaaa(iso: string): string {
  const day = iso.slice(8, 10);
  const month = iso.slice(5, 7);
  const year = iso.slice(0, 4);
  return `${day}.${month}.${year}`;
}

export function DlcBadge({ value, className }: DlcBadgeProps) {
  const bucket = dlcColor(value, new Date());
  return (
    <span
      className={cn(
        "inline-flex items-center px-2 py-0.5 rounded-md border text-xs font-medium",
        BUCKET_CLASSES[bucket],
        className,
      )}
    >
      {formatDateJjMmAaaa(value)}
    </span>
  );
}
```

Locked: the four bucket class strings come verbatim from 03-UI-SPEC.md §`<DlcBadge>`. Phase 1 §Color §Semantic locks the same values — do NOT inline these classes anywhere else in the codebase (UI-SPEC rule). The `formatDateJjMmAaaa` helper stays local to this file because it operates on a known ISO `YYYY-MM-DD` string from the store; the broader `formatDate(iso)` lives in `lib/raw-materials.ts` (file 5).

---

**File 2: `components/empty-state.tsx`** (per 03-UI-SPEC.md §Empty State + §Phase 3 instance). Server component (no client APIs needed; `onClick` on the optional CTA forces only the consuming context to be a client component, which it already is).

```tsx
import type { LucideIcon } from "lucide-react";
import { cn } from "@/lib/utils";
import { Button } from "@/components/ui/button";

type EmptyStateProps = {
  icon: LucideIcon;
  heading: string;
  body: string;
  cta?: { label: string; onClick: () => void; icon?: LucideIcon };
  className?: string;
};

export function EmptyState({ icon: Icon, heading, body, cta, className }: EmptyStateProps) {
  const CtaIcon = cta?.icon;
  return (
    <div
      className={cn(
        "flex flex-col items-center justify-center text-center",
        "py-16 px-4 border border-dashed border-border rounded-md bg-background",
        className,
      )}
    >
      <Icon size={48} className="text-muted-foreground mb-4" aria-hidden="true" />
      <h2 className="text-xl font-semibold text-foreground mb-2">{heading}</h2>
      <p className={cn("text-sm text-muted-foreground max-w-md", cta ? "mb-6" : "mb-0")}>
        {body}
      </p>
      {cta ? (
        <Button onClick={cta.onClick} className="gap-2">
          {CtaIcon ? <CtaIcon size={16} aria-hidden="true" /> : null}
          <span>{cta.label}</span>
        </Button>
      ) : null}
    </div>
  );
}
```

Locked: dashed border, `py-16`, `text-xl font-semibold mb-2`, `mb-6` when CTA is present (UI-SPEC §Empty State Layout classes). Note the component is "use client"-free even though `onClick` is in props — Next.js allows passing event handlers from a client parent, so this stays server-render-eligible.

---

**File 3: `components/ui/date-picker.tsx`** (per 03-UI-SPEC.md §`<DatePicker />` — custom). Client component (Popover + state). Locale must be French.

```tsx
"use client";

import * as React from "react";
import { CalendarIcon } from "lucide-react";
import { fr } from "date-fns/locale";
import { Button } from "@/components/ui/button";
import { Calendar } from "@/components/ui/calendar";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { cn } from "@/lib/utils";

type DatePickerProps = {
  value: string | undefined; // ISO YYYY-MM-DD
  onChange: (iso: string | undefined) => void;
  placeholder?: string;
  disabled?: (date: Date) => boolean;
  className?: string;
};

function isoToDate(iso: string | undefined): Date | undefined {
  if (!iso) return undefined;
  const d = new Date(`${iso}T00:00:00.000Z`);
  return Number.isNaN(d.getTime()) ? undefined : d;
}

function dateToIso(d: Date): string {
  // Always emit UTC date — matches lib/dlc.ts conventions
  return d.toISOString().slice(0, 10);
}

function formatJjMmAaaa(iso: string): string {
  return `${iso.slice(8, 10)}.${iso.slice(5, 7)}.${iso.slice(0, 4)}`;
}

export function DatePicker({
  value,
  onChange,
  placeholder = "JJ.MM.AAAA",
  disabled,
  className,
}: DatePickerProps) {
  const [open, setOpen] = React.useState(false);
  const selected = isoToDate(value);
  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button
          type="button"
          variant="outline"
          className={cn("w-full justify-start font-normal h-9", className)}
        >
          <span className={value ? "text-foreground" : "text-muted-foreground"}>
            {value ? formatJjMmAaaa(value) : placeholder}
          </span>
          <CalendarIcon size={16} className="ml-auto" aria-hidden="true" />
        </Button>
      </PopoverTrigger>
      <PopoverContent align="start" sideOffset={4} className="p-0">
        <Calendar
          mode="single"
          locale={fr}
          selected={selected}
          onSelect={(d) => {
            if (!d) return;
            onChange(dateToIso(d));
            setOpen(false);
          }}
          disabled={disabled}
          initialFocus
        />
      </PopoverContent>
    </Popover>
  );
}
```

Locked: trigger shape `<Button variant="outline" w-full justify-start font-normal h-9>` (UI-SPEC §DatePicker), placeholder `JJ.MM.AAAA`, `align="start" sideOffset={4}`, `p-0` on PopoverContent, `locale={fr}` from `date-fns/locale` (UI-SPEC explicit). Strings are stored as ISO `YYYY-MM-DD` (matches store/types/dlc).

If the shadcn `Calendar` block expects a different API in 2.10 (some versions changed `onSelect` signature for single-mode to `(date: Date | undefined) => void`), keep the `if (!d) return;` guard intact — that signature is the only one this code handles.

---

**File 4: `components/ui/combobox.tsx`** (per 03-UI-SPEC.md §`<Combobox />` — custom). Client component. Free-text fallback: even when no option matches, the typed value is returned.

```tsx
"use client";

import * as React from "react";
import { Check, ChevronsUpDown } from "lucide-react";
import { Button } from "@/components/ui/button";
import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { cn } from "@/lib/utils";

type ComboboxProps = {
  value: string;
  onChange: (next: string) => void;
  options: string[];
  placeholder?: string;
  searchPlaceholder?: string;
  emptyMessage?: string;
  className?: string;
};

export function Combobox({
  value,
  onChange,
  options,
  placeholder = "Sélectionnez ou saisissez une valeur",
  searchPlaceholder = "Rechercher...",
  emptyMessage = "Aucun résultat.",
  className,
}: ComboboxProps) {
  const [open, setOpen] = React.useState(false);
  const [query, setQuery] = React.useState("");
  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button
          type="button"
          variant="outline"
          role="combobox"
          aria-expanded={open}
          className={cn("w-full justify-start font-normal h-9", className)}
        >
          <span className={value ? "text-foreground" : "text-muted-foreground"}>
            {value || placeholder}
          </span>
          <ChevronsUpDown size={16} className="ml-auto" aria-hidden="true" />
        </Button>
      </PopoverTrigger>
      <PopoverContent
        align="start"
        sideOffset={4}
        className="w-[var(--radix-popover-trigger-width)] p-0"
      >
        <Command>
          <CommandInput
            placeholder={searchPlaceholder}
            value={query}
            onValueChange={(v) => {
              setQuery(v);
              onChange(v); // free-text fallback: typed value is the field value
            }}
          />
          <CommandList>
            <CommandEmpty>{emptyMessage}</CommandEmpty>
            <CommandGroup>
              {options.map((opt) => (
                <CommandItem
                  key={opt}
                  value={opt}
                  onSelect={(selected) => {
                    onChange(selected);
                    setQuery(selected);
                    setOpen(false);
                  }}
                >
                  <Check
                    size={16}
                    className={cn("mr-2", value === opt ? "opacity-100" : "opacity-0")}
                    aria-hidden="true"
                  />
                  {opt}
                </CommandItem>
              ))}
            </CommandGroup>
          </CommandList>
        </Command>
      </PopoverContent>
    </Popover>
  );
}
```

Locked: trigger shape matches DatePicker, `w-[var(--radix-popover-trigger-width)] p-0` so the popover width matches the trigger (UI-SPEC §Combobox). Free-text fallback (typing in `CommandInput` updates `onChange` directly) is the contract Phase 3 needs because suppliers may be brand new — UI-SPEC explicitly states "the user may pick an existing supplier OR keep typing".

The default `placeholder`, `searchPlaceholder`, and `emptyMessage` are GENERIC at this layer — Wave 2's reception dialog overrides them with the locked Phase 3 strings (`"Sélectionnez ou saisissez un fournisseur"`, `"Rechercher un fournisseur..."`, `"Aucun fournisseur — la valeur saisie sera enregistrée comme nouveau fournisseur."`). This component is reused by Phases 5 + 6 with their own copy.

---

**File 5: `lib/raw-materials.ts`** (per 03-CONTEXT.md §file layout + 03-UI-SPEC.md §Component Inventory). Pure helpers — no React, no client side-effects.

```tsx
import type { RawMaterial } from "./types";

export type StatutValue = "actif" | "epuise" | "dlc_depassee";

export const TYPE_LABELS: Record<RawMaterial["type"], string> = {
  boeuf: "Bœuf",
  agneau: "Agneau",
  poulet: "Poulet",
  epices: "Épices",
  marinade: "Marinade",
  autre: "Autre",
};

export const STATUT_LABELS: Record<StatutValue, string> = {
  actif: "Actif",
  epuise: "Épuisé",
  dlc_depassee: "DLC dépassée",
};

export const STATUT_CLASSES: Record<StatutValue, string> = {
  actif: "bg-emerald-100 text-emerald-800 border-emerald-200",
  epuise: "bg-zinc-100 text-zinc-600 border-zinc-200",
  dlc_depassee: "bg-red-100 text-red-800 border-red-200",
};

/**
 * Derive le statut d'un lot à partir de sa quantité restante et de sa DLC.
 * Précédence (UI-SPEC §Statut) : dlc_depassee > epuise > actif.
 *
 * Comparaison de dates en UTC midnight, identique à lib/dlc.ts.
 */
export function deriveStatut(rm: RawMaterial, today: Date): StatutValue {
  const dlcDay = new Date(`${rm.dlc.slice(0, 10)}T00:00:00.000Z`);
  const todayDay = new Date(`${today.toISOString().slice(0, 10)}T00:00:00.000Z`);
  if (dlcDay.getTime() < todayDay.getTime()) return "dlc_depassee";
  if (rm.quantiteRestante <= 0) return "epuise";
  return "actif";
}

/**
 * Liste dédupliquée et triée alphabétiquement des fournisseurs présents
 * dans le store, pour alimenter le combobox de réception.
 */
export function getSupplierOptions(rms: RawMaterial[]): string[] {
  const set = new Set<string>();
  for (const rm of rms) {
    const trimmed = rm.fournisseur.trim();
    if (trimmed) set.add(trimmed);
  }
  return Array.from(set).sort((a, b) => a.localeCompare(b, "fr"));
}

/**
 * Format ISO YYYY-MM-DD → JJ.MM.AAAA pour l'affichage Suisse-romand.
 * (Phase 1 inherited rule.)
 */
export function formatDate(iso: string): string {
  return `${iso.slice(8, 10)}.${iso.slice(5, 7)}.${iso.slice(0, 4)}`;
}
```

Locked: `TYPE_LABELS` keys match `RawMaterial["type"]` union exactly (`boeuf | agneau | poulet | epices | marinade | autre` per `lib/types.ts`); display labels follow UI-SPEC §Type select options (`Bœuf | Agneau | Poulet | Épices | Marinade | Autre`). `STATUT_LABELS` match UI-SPEC §Statut column (`Actif | Épuisé | DLC dépassée`). `STATUT_CLASSES` are the locked semantic palette from UI-SPEC §Statut variants.

`deriveStatut` precedence is explicitly UI-SPEC: `dlc_depassee` first (an expired lot is "DLC dépassée" even if quantity is 0), then `epuise`, then `actif`. The UTC-midnight comparison matches `lib/dlc.ts` exactly (same idiom from `dlcColor`).
  </action>
  <verify>
    <automated>cd /Users/sayanth/Desktop/viande &amp;&amp; test -f components/dlc-badge.tsx &amp;&amp; test -f components/empty-state.tsx &amp;&amp; test -f components/ui/date-picker.tsx &amp;&amp; test -f components/ui/combobox.tsx &amp;&amp; test -f lib/raw-materials.ts &amp;&amp; grep -q "export function DlcBadge" components/dlc-badge.tsx &amp;&amp; grep -q "export function EmptyState" components/empty-state.tsx &amp;&amp; grep -q "export function DatePicker" components/ui/date-picker.tsx &amp;&amp; grep -q "export function Combobox" components/ui/combobox.tsx &amp;&amp; grep -q "export function deriveStatut" lib/raw-materials.ts &amp;&amp; grep -q "export function getSupplierOptions" lib/raw-materials.ts &amp;&amp; grep -q "export function formatDate" lib/raw-materials.ts &amp;&amp; grep -q "TYPE_LABELS" lib/raw-materials.ts &amp;&amp; grep -q 'from "@/lib/dlc"' components/dlc-badge.tsx &amp;&amp; grep -q "border-dashed" components/empty-state.tsx &amp;&amp; grep -q "locale={fr}" components/ui/date-picker.tsx &amp;&amp; grep -q "radix-popover-trigger-width" components/ui/combobox.tsx &amp;&amp; for f in components/dlc-badge.tsx components/empty-state.tsx components/ui/date-picker.tsx components/ui/combobox.tsx lib/raw-materials.ts; do test "$(wc -l &lt; "$f")" -le 300 || { echo "$f exceeds 300 lines"; exit 1; }; done &amp;&amp; ! grep -rn ": any" components/dlc-badge.tsx components/empty-state.tsx components/ui/date-picker.tsx components/ui/combobox.tsx lib/raw-materials.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>All five files exist, export the named members listed above, contain no `: any`, each ≤ 300 lines, all locked classes / strings present, and `npx tsc --noEmit` exits 0. The shadcn `Calendar`, `Popover`, `Command`, `Button` imports resolve. `lib/raw-materials.ts` imports from `./types` and exports `deriveStatut`, `getSupplierOptions`, `formatDate`, `TYPE_LABELS`, `STATUT_LABELS`, `STATUT_CLASSES`, `StatutValue`.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# 1. shadcn primitives present
ls components/ui/{table,dialog,input,select,label,form,badge,calendar,popover,command}.tsx

# 2. Reusable primitives present
ls components/{dlc-badge,empty-state}.tsx components/ui/{date-picker,combobox}.tsx lib/raw-materials.ts

# 3. Peer deps resolved
node -e "['react-hook-form','@hookform/resolvers','zod','date-fns','react-day-picker'].forEach(p=>require.resolve(p))"

# 4. TypeScript clean
npx tsc --noEmit

# 5. No file > 300 lines (DEC-file-size-cap)
find components lib -name "*.tsx" -o -name "*.ts" | while read f; do
  lines=$(wc -l < "$f")
  [ "$lines" -gt 300 ] && echo "OVERSIZE: $f ($lines lines)"
done
# Expected: no output

# 6. Locked DLC bucket classes present (regression guard for UI-SPEC §DlcBadge)
grep -q "bg-emerald-100 text-emerald-800 border-emerald-200" components/dlc-badge.tsx
grep -q "bg-amber-100 text-amber-800 border-amber-200" components/dlc-badge.tsx
grep -q "bg-red-100 text-red-800 border-red-200" components/dlc-badge.tsx
grep -q "bg-zinc-100 text-zinc-600 border-zinc-200" components/dlc-badge.tsx

# 7. No DLC bucket classes inlined OUTSIDE of dlc-badge.tsx and raw-materials.ts (raw-materials.ts owns Statut palette which legitimately reuses red+grey+emerald)
EXPECTED_OWNERS="components/dlc-badge.tsx
lib/raw-materials.ts"
ACTUAL=$(grep -rln "bg-emerald-100 text-emerald-800 border-emerald-200" components lib app 2>/dev/null | sort)
diff <(echo "$EXPECTED_OWNERS" | sort) <(echo "$ACTUAL")
# Expected: no diff
```

Phase 3 page route is INTENTIONALLY untouched in Wave 1. Wave 2 (`03-02-PLAN.md`) replaces `app/matieres-premieres/page.tsx`.
</verification>

<success_criteria>
1. `npx shadcn@2.10.0 add table dialog input select label form badge calendar popover command --yes` executed successfully; the 10 expected files exist under `components/ui/`.
2. `react-hook-form`, `@hookform/resolvers`, `zod`, `date-fns`, `react-day-picker` are listed in `package.json` dependencies and resolve via Node.
3. `components/dlc-badge.tsx` exports `DlcBadge`, calls `dlcColor(value, new Date())`, renders the four locked bucket classes from UI-SPEC §DlcBadge, and formats the date as `JJ.MM.AAAA`.
4. `components/empty-state.tsx` exports `EmptyState`, renders the dashed-border container with optional CTA per UI-SPEC §Empty State.
5. `components/ui/date-picker.tsx` exports `DatePicker`, mounts `<Popover>` + `<Calendar mode="single" locale={fr}>`, emits ISO `YYYY-MM-DD` strings, accepts a `disabled` predicate.
6. `components/ui/combobox.tsx` exports `Combobox`, mounts `<Popover>` + `<Command>` with `w-[var(--radix-popover-trigger-width)]`, supports free-text fallback (typed query is propagated via `onChange`).
7. `lib/raw-materials.ts` exports `deriveStatut`, `getSupplierOptions`, `formatDate`, `TYPE_LABELS`, `STATUT_LABELS`, `STATUT_CLASSES`, `StatutValue`. Statut precedence is `dlc_depassee` → `epuise` → `actif`.
8. `npx tsc --noEmit` exits 0.
9. Every file added or modified in this plan ≤ 300 lines.
10. No `: any` anywhere in the new files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-matieres-premieres-screen/03-01-SUMMARY.md` summarizing files created, shadcn primitives installed, peer deps added, locked classes/strings honored, file-size headroom, and the contract Wave 2 inherits (specifically: which exports `<RawMaterialsTable />` and `<ReceptionDialog />` will import from each new file).
</output>
